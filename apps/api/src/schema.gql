# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type AdminAnalytics {
  averageBookingValue: Float!
  averageStayNights: Float!
  bookingsBySource: [BookingsBySource!]!
  bookingsByStatus: [BookingsByStatus!]!
  monthlyData: [MonthlyDataPoint!]!
  roomTypePopularity: [RoomTypePopularity!]!
}

type AdminDashboardStats {
  monthlyBookings: Int!
  monthlyRevenue: Float!
  occupancyRate: Int!
  occupiedRooms: Int!
  recentBookings: [Booking!]!
  todayCheckIns: Int!
  todayCheckOuts: Int!
  totalBookings: Int!
  totalRevenue: Float!
  totalRooms: Int!
}

input ApplyPriceSuggestionsInput {
  roomTypeId: String!
  suggestions: [PriceApplyItem!]!
}

type ApplySuggestionsResult {
  applied: Int!
  skipped: Int!
}

"""Auth response with tokens"""
type AuthResponse {
  accessToken: String
  message: String
  refreshToken: String
  success: Boolean!
  user: User
}

type AvailabilityCalendar {
  basePriceDaily: Float!
  calendar: [CalendarDay!]!
  roomTypeId: String!
  roomTypeName: String!
  totalRooms: Int!
}

"""Booking entity"""
type Booking {
  assignedRoomId: String
  bookingNumber: String!
  bookingType: BookingType!
  cancellationReason: String
  cancelledAt: DateTime
  checkInDate: DateTime!
  checkInTime: String
  checkOutDate: DateTime
  checkOutTime: String
  commissionAmount: Float!
  createdAt: DateTime!
  discountAmount: Float!
  extraGuestTotal: Float!
  guestEmail: String!
  guestId: String!
  guestName: String!
  guestPhone: String!
  hotel: BookingHotel
  hotelId: String!
  hotelPayout: Float!
  id: ID!
  numExtraGuests: Int!
  numGuests: Int!
  numHours: Int
  numRooms: Int!
  paymentStatus: PaymentStatus!
  payments: [Payment!]
  roomTotal: Float!
  roomType: BookingRoomType
  roomTypeId: String!
  source: BookingSource!
  specialRequests: String
  status: BookingStatus!
  taxes: Float!
  totalAmount: Float!
  updatedAt: DateTime!
}

"""Filters for listing bookings"""
input BookingFiltersInput {
  bookingType: BookingType
  fromDate: DateTime
  guestId: String
  hotelId: String
  search: String
  status: BookingStatus
  toDate: DateTime
}

type BookingHotel {
  address: String!
  city: String!
  id: ID!
  name: String!
  phone: String!
  slug: String!
}

"""Booking model type"""
enum BookingModel {
  BOTH
  DAILY
  HOURLY
}

"""Pagination for bookings list"""
input BookingPaginationInput {
  limit: Int! = 20
  page: Int! = 1
}

"""Booking creation result"""
type BookingResult {
  booking: Booking
  message: String
  paymentAmount: Float
  paymentOrderId: String
  success: Boolean!
}

type BookingRoomType {
  id: ID!
  images: [String!]!
  name: String!
  slug: String!
}

"""Source of the booking"""
enum BookingSource {
  BLUESTAY
  DIRECT
  WALK_IN
}

"""Status of the booking"""
enum BookingStatus {
  CANCELLED
  CHECKED_IN
  CHECKED_OUT
  CONFIRMED
  NO_SHOW
  PENDING
}

"""Type of booking (daily or hourly)"""
enum BookingType {
  DAILY
  HOURLY
}

type BookingsBySource {
  count: Int!
  source: String!
}

type BookingsByStatus {
  count: Int!
  status: String!
}

input BulkInventoryUpdateInput {
  """Override available count"""
  availableCount: Int
  endDate: DateTime!

  """Close/open dates for booking"""
  isClosed: Boolean

  """Minimum stay nights"""
  minStayNights: Int

  """Price override for the date range (null = use base price)"""
  priceOverride: Float
  roomTypeId: ID!
  startDate: DateTime!
}

type BulkSettleResult {
  count: Int!
}

type BulkUpdateResult {
  daysUpdated: Int!
  message: String!
  success: Boolean!
}

type CalendarDay {
  available: Int!
  date: String!
  isClosed: Boolean!
  price: Float!
}

"""Input for cancelling a booking"""
input CancelBookingInput {
  bookingId: String!
  reason: String!
}

"""Change password for logged in user"""
input ChangePasswordInput {
  currentPassword: String!
  newPassword: String!
}

"""Input for checking room availability"""
input CheckAvailabilityInput {
  checkInDate: DateTime!
  checkOutDate: DateTime
  hotelId: String!
  numGuests: Int! = 2
  numRooms: Int! = 1
  roomTypeId: String
}

"""Input for checking hourly slot availability"""
input CheckHourlyAvailabilityInput {
  date: DateTime!
  hotelId: String!
  numHours: Int! = 3
  numRooms: Int! = 1
  roomTypeId: String
  startTime: String
}

type CityPerformance {
  bookings: Int!
  city: String!
  revenue: Float!
}

type CommissionBookingInfo {
  bookingNumber: String
  guestName: String
  status: String
  totalAmount: Float
}

type CommissionEntry {
  bookingAmount: Float!
  bookingId: String!
  bookingInfo: CommissionBookingInfo
  commissionAmount: Float!
  commissionRate: Float!
  createdAt: DateTime!
  hotelId: String!
  hotelName: String
  id: ID!
  settledAt: DateTime
  status: String!
}

input CommissionsFilter {
  hotelId: String
  limit: Int
  page: Int
  status: String
}

type CommissionsList {
  commissions: [CommissionEntry!]!
  hasMore: Boolean!
  limit: Int!
  page: Int!
  total: Int!
  totalBookingAmount: Float!
  totalCommissionAmount: Float!
}

type ConfirmPaymentResult {
  bookingId: String
  bookingNumber: String
  message: String!
  success: Boolean!
}

"""Input for creating a daily booking"""
input CreateDailyBookingInput {
  checkInDate: DateTime!
  checkOutDate: DateTime!
  couponCode: String
  guestInfo: GuestInfoInput!
  hotelId: String!
  numExtraGuests: Int! = 0
  numGuests: Int! = 2
  numRooms: Int! = 1
  roomTypeId: String!
  source: BookingSource! = BLUESTAY
  specialRequests: String
}

"""Input for creating an hourly booking"""
input CreateHourlyBookingInput {
  checkInTime: String!
  date: DateTime!
  guestInfo: GuestInfoInput!
  hotelId: String!
  numGuests: Int! = 2
  numHours: Int!
  numRooms: Int! = 1
  roomTypeId: String!
  source: BookingSource! = BLUESTAY
  specialRequests: String
}

input CreateReviewInput {
  bookingId: ID!
  comment: String
  photos: [String!] = []
  rating: Int!
  title: String
}

input CreateRoomTypeInput {
  amenities: [String!]! = []
  basePriceDaily: Float!
  basePriceHourly: Float
  description: String
  extraGuestCharge: Float! = 0
  hotelId: ID!
  images: [String!]! = []
  maxExtraGuests: Int! = 0
  maxGuests: Int! = 2
  name: String!
  slug: String!
  sortOrder: Int! = 0
  totalRooms: Int! = 1
}

type DailyAvailabilityResult {
  checkIn: DateTime!
  checkOut: DateTime!
  hotelId: String!
  nights: Int!
  numRooms: Int!
  roomTypes: [RoomTypeAvailability!]!
  unavailableRoomTypes: [RoomTypeAvailability!]!
}

type DateRange {
  from: String!
  to: String!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type DeactivateResponse {
  message: String!
  success: Boolean!
}

type DeleteResult {
  message: String!
  success: Boolean!
}

"""Guest information for booking"""
input GuestInfoInput {
  email: String!
  name: String!
  phone: String!
}

"""Hotel entity"""
type Hotel {
  address: String!
  averageRating: Float
  bookingModel: BookingModel!
  checkInTime: String!
  checkOutTime: String!
  city: String!
  commissionRate: Float
  country: String!
  createdAt: DateTime!
  description: String
  domains: [HotelDomain!]
  email: String!
  heroImageUrl: String
  hourlyMaxHours: Int!
  hourlyMinHours: Int!
  id: ID!
  isActive: Boolean!
  isFeatured: Boolean!
  latitude: Float
  logoUrl: String
  longitude: Float
  name: String!
  phone: String!
  pincode: String!
  reviewCount: Int
  reviews: [Review!]
  roomTypes: [RoomType!]
  slug: String!
  starRating: Int!
  startingPrice: Float
  state: String!
  themeConfig: JSON
  updatedAt: DateTime!
  whatsapp: String
}

type HotelContentType {
  description: String
  heroImageUrl: String
  id: ID!
  logoUrl: String
  name: String!
  themeConfig: JSON
}

"""Hotel domain mapping"""
type HotelDomain {
  domain: String!
  id: ID!
  isPrimary: Boolean!
  sslStatus: String!
}

input HotelFiltersInput {
  amenities: [String!]
  checkInDate: DateTime
  checkOutDate: DateTime
  city: String
  guests: Int
  isFeatured: Boolean
  maxPrice: Float
  minPrice: Float
  minRating: Int
  search: String
  state: String
}

input HotelPaginationInput {
  limit: Int! = 12
  page: Int! = 1
  sortBy: HotelSortBy = FEATURED
  sortOrder: SortOrder = DESC
}

input HotelReplyInput {
  reply: String!
  reviewId: ID!
}

"""Field to sort hotels by"""
enum HotelSortBy {
  CREATED_AT
  FEATURED
  NAME
  PRICE
  RATING
}

type HourlyAvailabilityResult {
  date: DateTime!
  hotelId: String!
  numHours: Int!
  numRooms: Int!
  roomTypes: [HourlyRoomTypeAvailability!]!
}

type HourlyRoomTypeAvailability {
  isAvailable: Boolean!
  maxHours: Int!
  minHours: Int!
  roomType: HourlyRoomTypeSummary!
  slots: [HourlySlotInfo!]!
}

type HourlyRoomTypeSummary {
  amenities: [String!]
  basePriceHourly: Float
  id: ID!
  images: [String!]
  name: String!
}

type HourlySlotInfo {
  available: Int!
  endTime: String!
  isClosed: Boolean!
  price: Float!
  startTime: String!
}

type InitiatePaymentResult {
  amount: Float!
  bookingNumber: String!
  currency: String!
  gateway: String!
  gatewayData: String
  orderId: String!
  paymentId: String!
}

type InventoryCalendar {
  basePriceDaily: Float!
  calendar: [InventoryCalendarDay!]!
  roomTypeId: String!
  roomTypeName: String!
  totalRooms: Int!
}

type InventoryCalendarDay {
  available: Int!
  basePrice: Float!
  date: String!
  hasCustomAvailability: Boolean!
  hasCustomPrice: Boolean!
  isClosed: Boolean!
  minStayNights: Int!
  price: Float!
}

"""
The `JSON` scalar type represents JSON values as specified by [ECMA-404](http://www.ecma-international.org/publications/files/ECMA-ST/ECMA-404.pdf).
"""
scalar JSON @specifiedBy(url: "http://www.ecma-international.org/publications/files/ECMA-ST/ECMA-404.pdf")

"""Login with email and password"""
input LoginInput {
  email: String!
  password: String!
}

type LogoutResponse {
  message: String!
  success: Boolean!
}

type MonthlyDataPoint {
  bookings: Int!
  month: String!
  revenue: Float!
}

type Mutation {
  """Apply pricing suggestions to room inventory"""
  applyPriceSuggestions(input: ApplyPriceSuggestionsInput!): ApplySuggestionsResult!

  """Bulk update pricing and availability for a date range"""
  bulkUpdateInventory(input: BulkInventoryUpdateInput!): BulkUpdateResult!

  """Cancel a booking"""
  cancelBooking(input: CancelBookingInput!): Booking!

  """Change password for logged in user"""
  changePassword(input: ChangePasswordInput!): LogoutResponse!

  """Confirm payment after gateway processing"""
  confirmPayment(gatewayPaymentId: String, gatewaySignature: String, paymentId: ID!): ConfirmPaymentResult!

  """Create a daily booking"""
  createDailyBooking(input: CreateDailyBookingInput!): BookingResult!

  """Create an hourly booking"""
  createHourlyBooking(input: CreateHourlyBookingInput!): BookingResult!

  """Submit a review for a completed booking"""
  createReview(input: CreateReviewInput!): Review!

  """Create a new room type"""
  createRoomType(input: CreateRoomTypeInput!): RoomType!

  """Deactivate current user's account"""
  deactivateMyAccount: DeactivateResponse!

  """Deactivate a room type (soft delete)"""
  deleteRoomType(id: ID!): DeleteResult!

  """Delete SEO meta entry"""
  deleteSeoMeta(id: ID!): DeleteResult!

  """Start payment process for a booking"""
  initiatePayment(bookingId: ID!, method: String! = "card"): InitiatePaymentResult!

  """Login with email and password"""
  login(input: LoginInput!): AuthResponse!

  """Logout and revoke refresh token"""
  logout: LogoutResponse!

  """Self-serve hotel onboarding - creates hotel + admin account"""
  onboardHotel(input: OnboardHotelInput!): OnboardHotelResult!

  """Approve a pending review"""
  platformApproveReview(reviewId: ID!): Review!

  """Bulk settle multiple commissions"""
  platformBulkSettleCommissions(commissionIds: [ID!]!): BulkSettleResult!

  """Delete a review"""
  platformDeleteReview(reviewId: ID!): PlatformDeleteResult!

  """Mark a commission as settled"""
  platformSettleCommission(commissionId: ID!): CommissionEntry!

  """Activate or deactivate a hotel"""
  platformToggleHotelActive(hotelId: ID!, isActive: Boolean!): Hotel!

  """Toggle hotel featured status"""
  platformToggleHotelFeatured(hotelId: ID!, isFeatured: Boolean!): Hotel!

  """Update hotel commission rate"""
  platformUpdateHotelCommission(commissionRate: Float!, hotelId: ID!): Hotel!

  """Process refund for a cancelled booking"""
  processRefund(amount: Float, bookingId: ID!): RefundResult!

  """Refresh access token using refresh token"""
  refreshToken(input: RefreshTokenInput!): AuthResponse!

  """Register a new user"""
  register(input: RegisterInput!): AuthResponse!

  """Hotel admin replies to a guest review"""
  replyToReview(input: HotelReplyInput!): Review!

  """Request OTP for phone login"""
  requestOTP(input: RequestOTPInput!): OTPResponse!

  """Update booking status"""
  updateBookingStatus(input: UpdateBookingStatusInput!): Booking!

  """Update inventory for a single date"""
  updateDateInventory(input: SingleDateInventoryInput!): RoomInventory!

  """Update hotel details (admin only)"""
  updateHotel(input: UpdateHotelInput!): Hotel!

  """Update hotel content and theme"""
  updateHotelContent(input: UpdateHotelContentInput!): Hotel!

  """Update user profile"""
  updateProfile(input: UpdateProfileInput!): User!

  """Update an existing room type"""
  updateRoomType(input: UpdateRoomTypeInput!): RoomType!

  """Create or update SEO meta for a page"""
  upsertSeoMeta(input: UpsertSeoMetaInput!): SeoMetaType!

  """Verify OTP and login/register"""
  verifyOTP(input: VerifyOTPInput!): AuthResponse!
}

"""OTP send response"""
type OTPResponse {
  expiresAt: DateTime
  message: String!
  success: Boolean!
}

type OccupancyForecastDay {
  date: String!
  occupancyRate: Float!
  revenue: Float!
}

input OccupancyForecastInput {
  days: Int = 30
  hotelId: String!
}

input OnboardHotelInput {
  address: String!

  """Admin email (for login)"""
  adminEmail: String!

  """Admin full name"""
  adminName: String!

  """Admin password"""
  adminPassword: String!

  """Admin phone number"""
  adminPhone: String

  """DAILY | HOURLY | BOTH"""
  bookingModel: String = "DAILY"
  city: String!
  description: String

  """Custom domain (e.g. myhotel.in)"""
  domain: String

  """Hotel contact email"""
  hotelEmail: String!

  """Hotel name"""
  hotelName: String!

  """Hotel phone number"""
  phone: String!
  pincode: String!
  starRating: Int = 3
  state: String!
}

type OnboardHotelResult {
  adminEmail: String!
  hotelId: ID!
  hotelSlug: String!
  message: String!
  success: Boolean!
}

"""Paginated bookings result"""
type PaginatedBookings {
  bookings: [Booking!]!
  hasMore: Boolean!
  limit: Int!
  page: Int!
  total: Int!
}

"""Paginated hotels response"""
type PaginatedHotels {
  hasMore: Boolean!
  hotels: [Hotel!]!
  limit: Int!
  page: Int!
  total: Int!
}

"""Payment entity"""
type Payment {
  amount: Float!
  bookingId: String!
  createdAt: DateTime!
  currency: String!
  gateway: String!
  gatewayOrderId: String
  gatewayPaymentId: String
  id: ID!
  metadata: JSON
  refundAmount: Float!
  refundId: String
  status: String!
  updatedAt: DateTime!
}

"""Payment status of the booking"""
enum PaymentStatus {
  FAILED
  PAID
  PARTIALLY_REFUNDED
  PENDING
  REFUNDED
}

type PendingReviewEntry {
  comment: String
  createdAt: DateTime!
  guestEmail: String
  guestName: String
  hotelId: String!
  hotelName: String
  id: ID!
  rating: Int!
  title: String
}

type PendingReviewsList {
  hasMore: Boolean!
  reviews: [PendingReviewEntry!]!
  total: Int!
}

type PlatformAnalytics {
  cityPerformance: [CityPerformance!]!
  monthlyRevenue: [PlatformMonthlyData!]!
  sourceDistribution: [SourceDistribution!]!
  topHotels: [TopHotelData!]!
}

type PlatformDashboardStats {
  activeHotels: Int!
  bookingGrowth: Int!
  monthlyBookings: Int!
  monthlyRevenue: Float!
  pendingCommissions: Float!
  recentBookings: [Booking!]!
  recentHotels: [RecentHotelInfo!]!
  totalBookings: Int!
  totalCommissions: Float!
  totalGuests: Int!
  totalHotels: Int!
  totalRevenue: Float!
}

type PlatformDeleteResult {
  message: String!
  success: Boolean!
}

type PlatformHotel {
  address: String!
  adminsCount: Int
  averageRating: Float
  bookingModel: BookingModel!
  bookingsCount: Int
  checkInTime: String!
  checkOutTime: String!
  city: String!
  commissionRate: Float
  country: String!
  createdAt: DateTime!
  description: String
  domains: [HotelDomain!]
  email: String!
  heroImageUrl: String
  hourlyMaxHours: Int!
  hourlyMinHours: Int!
  id: ID!
  isActive: Boolean!
  isFeatured: Boolean!
  latitude: Float
  logoUrl: String
  longitude: Float
  name: String!
  phone: String!
  pincode: String!
  primaryDomain: String
  reviewCount: Int
  reviews: [Review!]
  reviewsCount: Int
  roomTypes: [RoomType!]
  roomTypesCount: Int
  slug: String!
  starRating: Int!
  startingPrice: Float
  state: String!
  themeConfig: JSON
  updatedAt: DateTime!
  whatsapp: String
}

input PlatformHotelsFilter {
  city: String
  isActive: Boolean
  limit: Int
  page: Int
  search: String
}

type PlatformHotelsList {
  hasMore: Boolean!
  hotels: [PlatformHotel!]!
  limit: Int!
  page: Int!
  total: Int!
}

type PlatformMonthlyData {
  bookings: Int!
  month: String!
  revenue: Float!
}

type PopularCity {
  city: String!
  hotelCount: Int!
  state: String!
}

input PriceApplyItem {
  date: String!
  price: Float!
}

type PriceSuggestionGQL {
  changePercent: Float!
  currentPrice: Float!
  date: String!
  demandLevel: String!
  occupancyRate: Float!
  reason: String!
  suggestedPrice: Float!
}

input PriceSuggestionsInput {
  fromDate: String!
  roomTypeId: String!
  toDate: String!
}

type PricingAnalysisGQL {
  averageOccupancy: Float!
  basePrice: Float!
  period: DateRange!
  revenue: RevenueProjection!
  roomTypeId: String!
  roomTypeName: String!
  suggestions: [PriceSuggestionGQL!]!
}

type Query {
  """Get hotel analytics data"""
  adminAnalytics(hotelId: ID!, months: Int = 6): AdminAnalytics!

  """Get dashboard stats for hotel admin"""
  adminDashboardStats(hotelId: ID!): AdminDashboardStats!

  """Get hotel content (description, images, theme)"""
  adminHotelContent(hotelId: ID!): HotelContentType!

  """Get inventory calendar for admin pricing management"""
  adminInventoryCalendar(endDate: DateTime!, roomTypeId: ID!, startDate: DateTime!): InventoryCalendar!

  """Get all room types for admin (including inactive)"""
  adminRoomTypes(hotelId: ID!): [RoomType!]!

  """Get all SEO meta entries for a hotel"""
  adminSeoMeta(hotelId: ID!): [SeoMetaType!]!

  """Get availability calendar for a room type"""
  availabilityCalendar(endDate: DateTime!, roomTypeId: ID!, startDate: DateTime!): AvailabilityCalendar!

  """Get booking by ID"""
  booking(id: ID!): Booking!

  """Get booking by booking number"""
  bookingByNumber(bookingNumber: String!): Booking!

  """List bookings with filters"""
  bookings(filters: BookingFiltersInput, pagination: BookingPaginationInput): PaginatedBookings!

  """Check if a booking can be reviewed"""
  canReviewBooking(bookingId: ID!): Boolean!

  """Check room availability for daily booking"""
  checkDailyAvailability(input: CheckAvailabilityInput!): DailyAvailabilityResult!

  """Check room availability for hourly booking"""
  checkHourlyAvailability(input: CheckHourlyAvailabilityInput!): HourlyAvailabilityResult!

  """Get featured hotels"""
  featuredHotels(limit: Float = 6): [Hotel!]!

  """Get hotel by ID"""
  hotel(id: ID!): Hotel!

  """Get all reviews for hotel (admin view, includes unpublished)"""
  hotelAllReviews: [Review!]!

  """Get hotel by domain for white-label sites"""
  hotelByDomain(domain: String!): Hotel

  """Get hotel by slug"""
  hotelBySlug(slug: String!): Hotel

  """Get review stats for a hotel"""
  hotelReviewStats(hotelId: ID!): ReviewStats!

  """Get published reviews for a hotel"""
  hotelReviews(hotelId: ID!, limit: Int = 10, page: Int = 1, sortBy: String = "newest"): ReviewsPage!

  """Get paginated hotels"""
  hotels(filters: HotelFiltersInput, pagination: HotelPaginationInput): PaginatedHotels!

  """Get current logged in user"""
  me: User!

  """Get current user's bookings"""
  myBookings(limit: Int = 10, offset: Int = 0): [Booking!]!

  """Get all reviews submitted by the logged-in guest"""
  myGuestReviews: [Review!]!

  """Get current user's reviews"""
  myReviews(limit: Int = 10, offset: Int = 0): [Review!]!

  """Get occupancy forecast for the next N days"""
  occupancyForecast(input: OccupancyForecastInput!): [OccupancyForecastDay!]!

  """Get all payments for a booking"""
  paymentsByBooking(bookingId: ID!): [Payment!]!

  """Get platform-wide analytics"""
  platformAnalytics(months: Int = 6): PlatformAnalytics!

  """List commissions with filters"""
  platformCommissions(filters: CommissionsFilter): CommissionsList!

  """Get platform-wide dashboard statistics"""
  platformDashboardStats: PlatformDashboardStats!

  """List all hotels with management info"""
  platformHotels(filters: PlatformHotelsFilter): PlatformHotelsList!

  """Get reviews pending moderation"""
  platformPendingReviews(limit: Int = 20, page: Int = 1): PendingReviewsList!

  """Get popular cities"""
  popularCities(limit: Float = 10): [PopularCity!]!

  """Get AI-powered pricing suggestions for a room type"""
  priceSuggestions(input: PriceSuggestionsInput!): PricingAnalysisGQL!

  """Get room type by ID"""
  roomType(id: ID!): RoomType!

  """Get room types for a hotel"""
  roomTypes(filters: RoomTypeFiltersInput, hotelId: ID!): [RoomType!]!

  """Quick search hotels"""
  searchHotels(limit: Float = 10, query: String!): [Hotel!]!

  """Get current user profile"""
  userProfile: User!
}

type RecentHotelInfo {
  bookingsCount: Int!
  city: String!
  createdAt: DateTime!
  id: ID!
  isActive: Boolean!
  name: String!
  roomTypesCount: Int!
  state: String!
}

"""Refresh token input"""
input RefreshTokenInput {
  refreshToken: String!
}

type RefundResult {
  amount: Float
  message: String!
  refundId: String
  success: Boolean!
}

"""Register new user"""
input RegisterInput {
  email: String!
  name: String!
  password: String!
  phone: String
}

"""Request OTP for phone login"""
input RequestOTPInput {
  phone: String!
}

type RevenueProjection {
  current: Float!
  projected: Float!
  uplift: Float!
}

"""Guest review entity"""
type Review {
  bookingId: String!
  comment: String
  createdAt: DateTime!
  guest: ReviewGuest
  guestId: String!
  hotelId: String!
  hotelReply: String
  id: ID!
  isPublished: Boolean!
  isVerified: Boolean!
  photos: [String!]!
  rating: Int!
  title: String
  updatedAt: DateTime!
}

type ReviewGuest {
  avatarUrl: String
  name: String!
}

type ReviewStats {
  averageRating: Float!
  ratingDistribution: [Int!]!
  totalReviews: Int!
}

type ReviewsPage {
  page: Int!
  reviews: [Review!]!
  total: Int!
  totalPages: Int!
}

"""Room inventory for a specific date"""
type RoomInventory {
  availableCount: Int!
  date: DateTime!
  id: ID!
  isClosed: Boolean!
  minStayNights: Int!
  priceOverride: Float
  roomTypeId: String!
}

"""Room type entity"""
type RoomType {
  amenities: [String!]!
  availableTonight: Int
  basePriceDaily: Float!
  basePriceHourly: Float
  bookingModelOverride: BookingModel
  createdAt: DateTime!
  description: String
  extraGuestCharge: Float!
  hotelId: String!
  id: ID!
  images: [String!]!
  isActive: Boolean!
  maxExtraGuests: Int!
  maxGuests: Int!
  maxHours: Int
  minHours: Int
  name: String!
  slug: String!
  sortOrder: Int!
  totalRooms: Int!
  updatedAt: DateTime!
}

type RoomTypeAvailability {
  isAvailable: Boolean!
  minAvailable: Int!
  nights: Int!
  pricePerNight: Float!
  roomType: RoomTypeSummary!
  totalPrice: Float!
}

"""Input for fetching room types"""
input RoomTypeFiltersInput {
  amenities: [String!]
  hotelId: String
  includeHourly: Boolean
  maxPrice: Float
  minGuests: Int
  minPrice: Float
}

type RoomTypePopularity {
  bookings: Int!
  revenue: Float!
  roomTypeName: String!
}

type RoomTypeSummary {
  amenities: [String!]!
  basePriceDaily: Float!
  id: ID!
  images: [String!]!
  maxGuests: Int!
  name: String!
  slug: String!
}

type SeoMetaType {
  canonicalUrl: String
  createdAt: DateTime!
  customJsonLd: JSON
  hotelId: String!
  id: ID!
  metaDescription: String
  metaTitle: String
  ogImageUrl: String
  pageSlug: String!
  updatedAt: DateTime!
}

input SingleDateInventoryInput {
  availableCount: Int
  date: DateTime!
  isClosed: Boolean
  minStayNights: Int
  priceOverride: Float
  roomTypeId: ID!
}

"""Sort order"""
enum SortOrder {
  ASC
  DESC
}

type SourceDistribution {
  count: Int!
  revenue: Float!
  source: String!
}

type TopHotelData {
  city: String!
  hotelId: String!
  hotelName: String!
  totalBookings: Int!
  totalRevenue: Float!
}

"""Input for updating booking status"""
input UpdateBookingStatusInput {
  assignedRoomId: String
  bookingId: String!
  status: BookingStatus!
}

input UpdateHotelContentInput {
  description: String
  heroImageUrl: String
  hotelId: ID!
  logoUrl: String

  """Theme config: { primaryColor, fontFamily, etc. }"""
  themeConfig: JSON
}

input UpdateHotelInput {
  address: String
  checkInTime: String
  checkOutTime: String
  city: String
  description: String
  email: String
  heroImageUrl: String
  hotelId: ID!
  latitude: Float
  logoUrl: String
  longitude: Float
  name: String
  phone: String
  pincode: String
  starRating: Int
  state: String
  themeConfig: JSON
  whatsapp: String
}

"""Update user profile input"""
input UpdateProfileInput {
  avatarUrl: String
  email: String
  name: String
  phone: String
}

input UpdateRoomTypeInput {
  amenities: [String!]
  basePriceDaily: Float
  basePriceHourly: Float
  description: String
  extraGuestCharge: Float
  id: ID!
  images: [String!]
  isActive: Boolean
  maxExtraGuests: Int
  maxGuests: Int
  name: String
  sortOrder: Int
  totalRooms: Int
}

input UpsertSeoMetaInput {
  canonicalUrl: String
  customJsonLd: JSON
  hotelId: ID!
  metaDescription: String
  metaTitle: String
  ogImageUrl: String

  """
  Page identifier e.g. "homepage", "rooms", "contact"
  """
  pageSlug: String!
}

"""User entity"""
type User {
  avatarUrl: String
  createdAt: DateTime!
  email: String
  emailVerified: Boolean!
  hotelId: String
  id: ID!
  isActive: Boolean!
  lastLoginAt: DateTime
  name: String!
  phone: String
  phoneVerified: Boolean!
  role: UserRole!
  updatedAt: DateTime!
}

"""User role type"""
enum UserRole {
  GUEST
  HOTEL_ADMIN
  HOTEL_STAFF
  PLATFORM_ADMIN
}

"""Verify OTP"""
input VerifyOTPInput {
  otp: String!
  phone: String!
}